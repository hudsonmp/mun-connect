# Core AWS dependencies
boto3>=1.26.0
botocore>=1.29.0
aws-lambda-powertools>=2.18.0

# ML dependencies
torch>=2.0.0,<3.0.0  # Optional, smaller CPU-only version available with pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers>=4.30.0,<5.0.0
nltk>=3.8.1
numpy>=1.24.0,<2.0.0
textstat>=0.7.3

# AWS service integration
python-dotenv>=1.0.0  # For loading environment variables from .env files
requests>=2.28.0  # For making HTTP requests

# Testing and development
pytest>=7.3.1
pytest-mock>=3.10.0
pytest-cov>=4.1.0
moto>=4.1.0  # For mocking AWS services in tests
reportlab>=3.6.12  # For PDF generation in tests

# Lambda Layer compatibility
# For creating Lambda layers, these packages should be installed separately 
# with platform-specific wheels
# Example: pip install torch --index-url https://download.pytorch.org/whl/cpu -t layers/torch/python
# transformers, torch, numpy and tokenizers should be packaged as separate layers

# Container dependencies (for ECS deployment)
flask>=2.2.0
gunicorn>=20.1.0

# Optional: Performance optimization
pydantic>=2.0.0  # For data validation
orjson>=3.8.0  # Faster JSON serialization/deserialization
